# Get Analyst: Feather Rush

**Тестовое задание для платформы [GetAnalyst](https://getanalyst.ru/)** — брендированная
браузерная мини-игра: птичка летит через бесконечный летний уровень, проходит проходы
между скалами, брёвнами и лианами, собирает червячков, держит комбо и бьёт локальные
рекорды.

Никакой сборки, зависимостей и бэкенда: игра открывается двойным кликом по `index.html`
и полностью работает на HTML5 Canvas, vanilla JavaScript и Web Audio API.

## Как скачать и запустить игру — пошагово

Инструкция для компьютера с Windows, macOS или Linux. Устанавливать Git, Python
или Node.js для этого способа не нужно.

### 1. Скачайте игру

1. Откройте [страницу проекта на GitHub](https://github.com/GeekZick-commit/GetAnalyst-Bird).
2. Над списком файлов найдите переключатель ветки и выберите **main**, если выбрана другая ветка.
3. Нажмите зелёную кнопку **Code**, затем **Download ZIP**.
4. Дождитесь скачивания файла `GetAnalyst-Bird-main.zip`. Обычно он попадает в папку **«Загрузки»**.

Можно сразу нажать [**Скачать игру ZIP-архивом**](https://github.com/GeekZick-commit/GetAnalyst-Bird/archive/refs/heads/main.zip).

### 2. Распакуйте архив

- **Windows:** нажмите правой кнопкой по ZIP-файлу → **«Извлечь всё…»** → **«Извлечь»**.
- **macOS:** дважды нажмите на ZIP-файл — рядом появится распакованная папка.
- **Linux:** нажмите правой кнопкой по ZIP-файлу → **«Распаковать»** / **«Извлечь здесь»**
  (название пункта зависит от системы).

Откройте получившуюся папку `GetAnalyst-Bird-main`. В ней должны лежать
`index.html`, `styles.css` и папка `js`.
Распаковывайте **весь архив**: одного файла `index.html` для работы игры недостаточно.

### 3. Откройте игру

1. В распакованной папке дважды нажмите на `index.html`.
   Если расширения файлов скрыты, он может называться просто `index`.
2. Если открылся текстовый редактор, нажмите по файлу правой кнопкой →
   **«Открыть с помощью»** и выберите браузер, например Firefox, Chrome или Edge.
   Также можно перетащить файл в окно браузера.
3. Введите никнейм — имя игрока длиной от 2 до 16 символов.
4. Выберите птичку, нажмите **PLAY**, затем **пробел**, чтобы начать полёт.
   Повторные нажатия пробела поднимают птичку; клавиша **P** ставит игру на паузу.

Открывайте `index.html` **из распакованной папки**, а не из окна ZIP-архива.
Если виден только текст или пропало оформление, проверьте, что рядом с `index.html`
остались `styles.css` и папка `js`. Нажимать файл `index.html` на самой странице
GitHub не нужно: там показывается исходный код, а не запущенная игра.

Чтобы получить новую версию, снова скачайте ZIP и распакуйте его в новую папку.

## Ссылки

| | |
| --- | --- |
| Платформа | [getanalyst.ru](https://getanalyst.ru/) — ИТ-школа практических курсов по системному и бизнес-анализу |
| Исходный код | [github.com/GeekZick-commit/GetAnalyst-Bird](https://github.com/GeekZick-commit/GetAnalyst-Bird) |
| Ветка с работой | `develop` (она же влита в `main`) |

![Главное меню](docs/screenshot-menu.png)

![Геймплей](docs/screenshot-gameplay.png)

![Готовность к старту](docs/screenshot-ready.png)

![Game Over](docs/screenshot-gameover.png)

## Соответствие требованиям задания

| Блок задания | Как выполнено |
| --- | --- |
| Главное меню и брендинг | Крупный `GET ANALYST`, под ним `Feather Rush`, фирменная палитра `#16F3CE`, векторный знак бренда, CTA в акцентном цвете |
| Никнейм | 2–16 символов, буквы (включая кириллицу), цифры, `_`, `-`; пустой не пускает в игру |
| Выбор персонажа | Три визуально разных птички, карточки с preview и idle-анимацией, выбор сохраняется в `localStorage` |
| Баланс персонажей | Одинаковые импульс, гравитация, hitbox и очки — различия только визуальные (закреплено тестом) |
| Управление | `Space` / `↑` / `W` / клик / tap, плавный импульс и гравитация, пауза `P` / `Esc` |
| Жизни и урон | Три сердца, ~1.3 с неуязвимости с мерцанием, Game Over после третьего попадания |
| Препятствия | Пары «сверху/снизу» из скал, брёвен и лиан, проход всегда проходим, +10 за пролёт |
| Червячки | Пять типов: +10 / +25 / +50 / +100 / −25, редкость по весам, у золотого glow и свой звук |
| Комбо | x2 на 3-м, x3 на 6-м, x4 на 10-м; сброс при столкновении и на красном червячке, SCORE ≥ 0 |
| Рост сложности | Шесть стадий с плавной интерполяцией: скорость 265→520, проход 302→212, интервал 1.75→1.14 с |
| HUD | SCORE, BEST, сердца, COMBO, портрет и ник выбранной птички |
| Game Over | Player / Bird / Score / Best, `NEW HIGH SCORE` с конфетти, кнопки PLAY AGAIN, CHANGE BIRD, MAIN MENU |
| Лидерборд | TOP-10 в `localStorage`: ник, очки, птичка, дата; CLEAR SCORES с подтверждением |
| Мир и частицы | Параллакс в несколько слоёв, летнее небо; burst, sparkles, confetti, screen shake, trail у Cyber |
| AudioManager | Один `AudioContext`, каналы Master/Music/SFX, 17 звуков, зацикленная процедурная музыка |
| Настройки звука | MUSIC и SFX раздельно, mute, всё сохраняется; `AudioContext` создаётся только после жеста |
| Состояния | MENU / CHARACTER_SELECT / PLAYING / PAUSED / GAME_OVER, авто-пауза при потере фокуса и скрытии вкладки |
| Архитектура | 14 модулей: Game, Bird, BirdSelector, Obstacle, Worm, ParticleSystem, InputManager, AudioManager, ScoreManager, StorageManager, UIManager и вспомогательные |
| Адаптив | Сцена 16:9 масштабируется без искажений, карточки перестраиваются, мобильное управление |
| Edge cases | Пустой и слишком длинный ник, быстрое переключение птиц, рестарт, CHANGE BIRD, `localStorage`, resize, потеря фокуса, несколько столкновений подряд, одновременный сбор червячков, autoplay-ограничения, ошибки аудио |
| Производительность | 6.6 мс на кадр в Chromium, стабильные 60 fps в Firefox; динамическое разрешение и замеры обоих движков в репозитории |
| Проверяемость | В ходе разработки — 48 юнит-тестов логики и 163 интеграционные проверки в headless-браузере, плюс замеры кадрового времени в Chromium и Firefox |

## Быстрый старт

Backend не нужен, зависимостей нет.

```bash
# вариант 1 — просто открыть файл
xdg-open index.html          # или перетащить index.html в браузер

# вариант 2 — локальный сервер
python3 -m http.server 8080  # затем http://localhost:8080
```

Оба варианта проверены в headless Chromium: игра, звук и `localStorage` работают и по
`file://`, и по `http://`.

## Управление

| Действие | Клавиши |
| --- | --- |
| Взлёт (flap) | `Space`, `↑`, `W`, клик мышью, tap на мобильном |
| Пауза / продолжить | `P`, `Esc`, кнопка ❚❚ в HUD |
| Звук вкл/выкл | `M`, кнопка 🔊 в HUD |
| Счётчик FPS, масштаба рендера и частиц | `F` |
| Старт из поля nickname | `Enter` |

Потеря фокуса окна или скрытие вкладки автоматически ставит игру на паузу.

## Как играть

1. Ввести nickname (2–16 символов: буквы, цифры, `_`, `-`; поддерживаются кириллица и Unicode-буквы).
2. Выбрать одну из трёх птичек.
3. Нажать **PLAY** — игра переходит в состояние готовности: птичка парит на месте,
   уровень не движется, на экране горит `GET READY`.
4. Первое нажатие (`Space` / tap / клик) запускает полёт. Так игрок начинает игру
   в тот момент, когда сам готов, а не падает сразу после нажатия PLAY.

Птичка автоматически летит вперёд, игрок управляет только высотой. Три жизни, ~1.3 с
неуязвимости после столкновения (птичка мигает). После третьего попадания — **GAME OVER**.

### Червячки

| Червячок | SCORE | Редкость | Особенность |
| --- | --- | --- | --- |
| 🟢 Green | +10 | частый | — |
| 🔵 Blue | +25 | средний | — |
| 🟣 Purple | +50 | редкий | — |
| 🟡 Golden | +100 | очень редкий | glow, sparkle, отдельный бонус-звук |
| 🔴 Red | −25 | нечастый | ломает комбо, жизнь не отнимает, SCORE не уходит ниже 0 |

Проход между препятствиями даёт **+10**.

### Комбо

| Червячков подряд | Множитель |
| --- | --- |
| 3 | x2 |
| 6 | x3 |
| 10 | x4 |

Множитель применяется к SCORE за полезных червячков. Красный червячок или столкновение
сбрасывают комбо. На экране появляется `COMBO x2!`, `COMBO x3!`, `COMBO x4!`.

### Сложность

Шесть уровней сложности, привязанных к SCORE (0 / 150 / 400 / 800 / 1400 / 2200), значения
интерполируются плавно, без скачков: скорость растёт 265 → 520 px/s, проход сужается
302 → 212 px, интервал между препятствиями падает 1.75 → 1.14 с, меняется рисунок уровня.
Генерация гарантирует проходимость: минимальный проход 200 px, вертикальное смещение
соседних проходов ограничено физикой полёта, а автопилот-тест пролетает все стадии без
единого столкновения.

## Персонажи

| Персонаж | Характер | Внешний вид |
| --- | --- | --- |
| **SCOUT** | любопытный, дерзкий | компактная оранжевая птичка, красный хохолок, быстрый взмах |
| **PROFESSOR** | серьёзный, аналитичный, слегка комичный | сине-фиолетовая птичка в круглых очках, спокойная анимация |
| **CYBER** | футуристичный, уверенный | бирюзовая птичка со светящимся визором, геометрическим крылом и trail-эффектом |

Все три птички имеют **одинаковую механику**: тот же импульс взлёта, та же гравитация, тот
же hitbox (радиус 17 px), те же очки и жизни. Отличаются только спрайт, ритм взмаха и
косметические эффекты — лидерборд остаётся честным. Это закреплено автотестом
(`every character shares exactly the same flight model and hitbox`).

Анимации: idle-парение в меню, flap с наклоном вверх при взлёте, наклон вниз при падении,
короткая реакция на столкновение (вспышка + рывок), мерцание в период неуязвимости.
У Cyber при взлёте остаётся короткий светящийся след.

## Брендинг и дизайн-система

Палитра и типографика взяты из фирменного стиля GetAnalyst (логотип и сайт getanalyst.ru):

| Токен | Значение | Назначение |
| --- | --- | --- |
| `--ga-accent` | `#16F3CE` | фирменный бирюзовый акцент, CTA, SCORE, heart-статусы |
| `--ga-accent-2` / `--ga-accent-3` | `#0ACAA6` / `#08A285` | градиенты и тени |
| `--ga-bg` / `--ga-surface-2` | `#0B0E0F` / `#1C1C1C` | тёмные поверхности интерфейса |
| `--ga-text` / `--ga-muted` | `#FFFFFF` / `#8E8E8E` | контрастный и вторичный текст |
| `--ga-danger` / `--ga-gold` | `#FF5C7A` / `#FFD166` | опасность и рекорд |

Шрифт — Montserrat с системным fallback. Знак бренда (две дуги + два квадрата) воссоздан
векторно в SVG, wordmark `GET ANALYST` набран фирменным акцентом со свечением.

Мир игры — яркая мультяшная аркада (летнее небо, солнце, облака, холмы, деревья,
параллакс в 5 слоёв), интерфейс — тёмная технологичная стилистика Get Analyst. Размеры
всего UI считаются от переменной `--u` (1% ширины сцены), поэтому интерфейс
масштабируется вместе с canvas без искажения пропорций 16:9.

## Архитектура

```
/
├── index.html            # разметка всех экранов + подключение модулей
├── styles.css            # дизайн-система Get Analyst, адаптив, анимации UI
├── js/
│   ├── fart-sample.js    # записанный звук взмаха (data URI, работает по file://)
│   ├── utils.js          # математика, RNG, валидация nickname, эмиттер
│   ├── config.js         # палитра, физика, персонажи, червячки, стадии сложности
│   ├── storage.js        # StorageManager: localStorage + безопасный fallback
│   ├── score.js          # ScoreManager: очки, комбо, статистика, difficulty
│   ├── particles.js      # ParticleSystem: burst, ring, sparkle, confetti, trail, текст
│   ├── background.js     # ParallaxBackground: небо, солнце, облака, холмы, зелень
│   ├── bird.js           # Bird + отрисовка трёх персонажей, физика, анимации
│   ├── worm.js           # Worm/WormManager: 5 типов, редкость, спавн в проходах
│   ├── obstacle.js       # ObstaclePair/Manager: генерация проходов и структуры
│   ├── audio.js          # AudioManager: MasterGain/MusicGain/SfxGain, SFX, музыка
│   ├── input.js          # InputManager: клавиатура, мышь, touch, blur
│   ├── bird-selector.js  # BirdSelector: карточки персонажей и их preview
│   ├── ui.js             # UIManager: экраны, HUD, лидерборд, модалки, тосты
│   └── game.js           # Game: состояния, игровой цикл (delta time), коллизии, рендер
├── docs/                 # скриншоты для README
└── package.json          # только npm-скрипт запуска сервера, без зависимостей
```

Модули оформлены как UMD-совместимые: в браузере подключаются обычными `<script>` и
живут в пространстве `GA` (работает и по `file://`), в Node могут подключаться через
`require()` — этим пользовались автотесты во время разработки.

Ключевые технические решения:

* **Игровой цикл** — `requestAnimationFrame` с delta time, шаг ограничен 50 мс, вся физика
  и тайминги в секундах, поэтому игра не зависит от FPS (проверено тестом на двух разных
  размерах шага). Поверх этого работает динамическое разрешение: canvas рендерится в
  масштабе от 0.6× до 1.5× от мира 1280×720, контроллер опускает масштаб, если p90 или
  медиана интервалов кадров выходят за бюджет, и поднимает обратно, когда картинка
  стабильно плавная (с гистерезисом и прогревом, чтобы не дёргаться).
* **Состояния** — `MENU`, `CHARACTER_SELECT`, `PLAYING`, `PAUSED`, `GAME_OVER`.
* **Звук** — один `AudioContext`, три gain-канала (`MasterGain`, `MusicGain`, `SfxGain`).
  Взмах крыла озвучен **записанным сэмплом** (`js/fart-sample.js`): первый звук из
  присланного `man-farts.mp3`, обрезанный до 185 мс, моно, нормализованный в −1 dBFS
  и перекодированный в MP3 (3.2 КБ). Он встроен как data URI намеренно: по `file://`
  браузер запрещает читать соседний аудиофайл, а игра обязана работать при открытии
  `index.html` с диска. При проигрывании высота слегка рандомизируется (0.93–1.08×),
  чтобы серия взмахов не звучала одинаково. Если сэмпл не декодировался, автоматически
  используется синтезированный пердёж (`_fart`): две расстроенные пилообразные волны
  плюс суб-синус через lowpass, падающий с 1.1 кГц до 190 Гц, глубокий квадратный LFO
  по громкости, слой band-pass шума и двухступенчатая огибающая 0.42–0.66 с.
  Остальные SFX синтезируются осцилляторами и шумом. Музыка — процедурная (прогрессия Am–F–C–G, бас,
  арпеджио, перкуссия), но рендерится заранее в два зацикленных буфера и играет двумя
  источниками: громкость «плотного» варианта растёт вместе со сложностью, темп слегка
  ускоряется.
  `AudioContext` создаётся только после жеста пользователя (autoplay policy), при
  недоступности Web Audio игра продолжает работать без звука.
* **Хранилище** — `localStorage` с префиксом `ga.featherrush.v1.`: nickname, выбранная
  птичка, TOP-10 (хранится до 50 записей: nickname, score, bird, date, worms), настройки
  звука, счётчик игр. Битые данные, отключённое хранилище и private mode не ломают игру —
  используется in-memory fallback.

## Производительность

Кадровое время измерялось инструментом, который принудительно растрирует каждый кадр
(`getImageData`), наполняет сцену препятствиями, червячками и частицами и сравнивает
стоимость по слоям — отдельно в Chromium и в Firefox (у них разные canvas-бэкенды,
поэтому цифры одного движка ничего не говорят о другом). Именно эти замеры определили
оптимизации:

| Что было узким местом | Что сделано |
| --- | --- |
| `ctx.shadowBlur` на каждом сегменте каждого червячка и на каждой частице | заменён на заранее испечённые спрайты свечения (`drawImage`), у текстовых всплывашек размытие убрано совсем |
| Перерисовка ~2.8 млн пикселей фона за кадр: градиенты неба, солнца, холмов, деревьев создавались заново каждый кадр | каждый слой фона испечён один раз в offscreen-canvas; небо + солнце + дальние холмы блитятся одним непрозрачным `copy`-проходом, который заодно заменяет `clearRect`; движутся только три слоя (облака, ближние холмы, зелень) |
| Градиенты и `RNG` препятствий внутри кадра, двойная заливка теней на всю высоту колонны | градиенты кэшированы по стилю, бугры предвычислены при создании, тень рисуется полосой у прохода |
| Аллокации в горячем пути (прямоугольники коллизий, `{spawned,passed}`, объекты сложности, массивы сбора червячков) — источник периодических пауз GC | коллизии считаются без объектов, результаты переиспользуются, цели сложности пересчитываются 5 раз в секунду |
| Планировщик музыки создавал ~46 Web Audio-узлов в секунду с пиками до 2.3 мс на главном потоке | музыка рендерится заранее в два зацикленных буфера (`OfflineAudioContext`) и играет двумя источниками с кроссфейдом по сложности — 0 узлов в секунду во время игры; живой планировщик остался только как fallback |
| Canvas в HiDPI раздувался до ~4 млн пикселей | бюджет бэк-стора 1.6 млн пикселей: разрешение адаптивно снижается, картинка не искажается |
| Полный ре-рендер во время паузы | на паузе кадр не перерисовывается вообще |
| Дробный масштаб canvas (`scale 1.091`): браузер пересемплировал каждый блит и всю поверхность | бэк-стор рендерится в чистом масштабе от 1280×720, внутренние блиты идут 1:1, а браузер масштабирует готовый кадр один раз |
| На слабых машинах кадр всё равно не укладывался в бюджет | динамическое разрешение: контроллер качества следит за перцентилями интервалов кадров и опускает/поднимает масштаб рендера (0.6×…1.5×), в `F` видно текущий множитель |
| Тяжёлые CSS-эффекты поверх анимированного canvas (`backdrop-filter`, размытые тени на всю сцену) | убраны или упрощены: в Firefox они заставляют перечитывать и размывать canvas каждый кадр |
| Экран меню и выбора птички перерисовывали canvas 60 раз в секунду под большой DOM-панелью | в этих состояниях мир статичен (он всё равно затемнён оверлеем): браузер не перекомпоновывает всю сцену каждый кадр — меню 31 → **60 fps** |
| Оба селектора птичек крутили rAF-циклы одновременно, рисуя 6 canvas-превью (включая скрытый экран выбора) | анимируется только видимый селектор, превью обновляются на 30 fps, градиенты и аура кэшированы |
| Перезапуск CSS-анимаций через `void element.offsetWidth` в горячем пути (очки, комбо, сердца) | заменено на Web Animations API: нет принудительного синхронного layout несколько раз в секунду |
| `setIntensity` вызывался каждый кадр и каждый кадр ставил 4 события в AudioParam | значение применяется только при реальном изменении сложности |
| Портрет птички в HUD перерисовывался по таймеру | рисуется один раз при смене персонажа |

Измерения на двух движках (оба — программный растеризатор, то есть худший случай):

| Метрика | Chromium до | Chromium после | Firefox до | Firefox после |
| --- | --- | --- | --- | --- |
| Стоимость кадра геймплея | 11.4 мс | **7.0 мс** | 11.8 мс | **7.6 мс** |
| Живой FPS (среднее) | 62 | 62 | 46.8 | **58.1** |
| p95 интервала кадра | 21 мс | 19.8 мс | 39–43 мс | **22 мс** |
| Кадров тяжелее 33 мс | 0/80 | 0/80 | 9–13 из 90 | **0 из 90** |
| Экран меню (3 мин) | — | 60 fps | 31–41 fps, 20–30 тяжёлых кадров/с | **60 fps, 0 тяжёлых кадров** |

Решающим был трёхминутный прогон с разбивкой по секундам (FPS, кадры тяжелее 33 мс,
масштаб рендера, события): он показал, что «лаги в первые две минуты» создавал сам
контроллер качества — 15 переключений масштаба за 45 секунд, каждое из которых
пересоздаёт canvas и compositing-слой, а всплеск от пересоздания запускал следующее
понижение. Сейчас контроллер — аварийная страховка: не чаще одного изменения в
10 секунд, повышение качества только после ~12 секунд идеальных кадров.

Отдельно: фон **7.8 → 5.7 мс**, препятствия **3.0 → 2.7 мс**, музыка
**45.6 → 0** создаваемых Web Audio-узлов в секунду. Плюс `F` в игре показывает живой
FPS, текущий масштаб рендера, число частиц, червячков и препятствий.

## Как это проверялось

Проект разрабатывался вместе с автотестами; в поставку они не входят, чтобы в
репозитории осталась только игра, но их можно достать из истории git
(`git log -- tests`).

* **48 логических тестов на Node.** Утилиты и валидация ника, `localStorage`
  (включая битые данные, отключённое хранилище и private mode), счёт и комбо, «пол»
  очков, редкость пяти типов червячков, генерация проходимых препятствий — автопилот
  пролетает все шесть стадий сложности без единого столкновения, независимость физики
  от шага времени, идентичность всех трёх персонажей.
* **163 интеграционные проверки в реальном headless-браузере.** Загрузка и брендинг
  меню, валидация ника, выбор и сохранение птички, старт и ожидание первого взмаха,
  flap и границы мира, проход препятствия (+10), столкновение и потери жизней,
  неуязвимость, все пять типов червячков, комбо и его множители, «пол» очков, рост
  сложности, пауза и авто-пауза, Game Over, `NEW HIGH SCORE`, сохранение в
  `localStorage`, PLAY AGAIN / CHANGE BIRD / MAIN MENU, лидерборд после перезагрузки
  страницы, CLEAR SCORES с подтверждением, звук и mute, адаптив, отсутствие JS-ошибок
  и бюджет кадра.
* **Замеры производительности** в Chromium и Firefox, включая трёхминутные прогоны;
  результаты приведены выше.

Проверить оставшийся код можно вручную: открой `index.html`, нажми `F` — плашка
покажет живой FPS, текущий масштаб рендера, число частиц, червячков и препятствий.

## Ограничения

* Backend отсутствует: лидерборд локальный, рекорды видны только в этом браузере.
* Музыка и звуки синтезируются на лету — это осознанный выбор вместо внешних ассетов
  (нет copyrighted-контента и лишних файлов), поэтому звук не «студийный».
* Точных брендовых ассетов в репозитории не было: палитра, шрифт и знак бренда
  воссозданы по публичному фирменному стилю getanalyst.ru.
* Звук взмаха — присланный пользователем сэмпл (`man-farts.mp3`); права на него
  остаются на стороне заказчика. Остальные звуки и музыка синтезируются кодом.
* В меню и на экране выбора птички мир намеренно статичен: под затемняющим
  оверлеем движение не читается, а постоянная перерисовка canvas заставляла
  браузер перекомпоновывать всю сцену каждый кадр.
* Игра рассчитана на клавиатуру/тап; геймпад не поддерживается.
* Скриншоты в `docs/` сняты автоматически во время разработки.

## Лицензия

Не определена.
